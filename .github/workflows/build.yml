name: 🔥 Build Khanjar APK

on:
  push:
    branches: [ main ]
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest
    timeout-minutes: 60
    
    steps:
    - name: 📥 Checkout Repository
      uses: actions/checkout@v3
    
    - name: 🐍 Set up Python 3.10
      uses: actions/setup-python@v4
      with:
        python-version: '3.10'
    
    - name: 💾 Cache Buildozer & Dependencies
      uses: actions/cache@v3
      with:
        path: |
          ~/.buildozer
          .buildozer
          ~/.gradle
          ~/.android
        key: buildozer-${{ hashFiles('buildozer.spec') }}-v5
        restore-keys: |
          buildozer-
    
    - name: 📦 Install System Dependencies
      run: |
        sudo apt-get update
        sudo apt-get install -y \
          build-essential \
          libssl-dev \
          libffi-dev \
          openjdk-17-jdk \
          zlib1g-dev \
          ccache \
          autoconf \
          automake \
          libtool \
          wget \
          unzip
    
    - name: 🔧 Install Buildozer & Cython
      run: |
        pip install --upgrade pip
        pip install buildozer cython==0.29.36
    
    - name: 📜 Setup Android SDK Complete
      run: |
        export ANDROID_HOME=$HOME/.buildozer/android/platform/android-sdk
        
        # ساخت ساختار کامل
        mkdir -p $ANDROID_HOME/cmdline-tools
        mkdir -p $ANDROID_HOME/tools/bin
        
        cd $ANDROID_HOME/cmdline-tools
        
        # دانلود SDK Command Line Tools
        if [ ! -d "latest" ]; then
          echo "📥 Downloading Android SDK Command Line Tools..."
          wget -q https://dl.google.com/android/repository/commandlinetools-linux-9477386_latest.zip
          unzip -q commandlinetools-linux-9477386_latest.zip
          mv cmdline-tools latest
          rm commandlinetools-linux-9477386_latest.zip
          echo "✅ SDK Command Line Tools installed"
        else
          echo "✅ SDK Command Line Tools already cached"
        fi
        
        # ساخت Symlink برای مسیر قدیمی
        ln -sf $ANDROID_HOME/cmdline-tools/latest/bin/sdkmanager $ANDROID_HOME/tools/bin/sdkmanager
        ln -sf $ANDROID_HOME/cmdline-tools/latest/bin/avdmanager $ANDROID_HOME/tools/bin/avdmanager
        
        # تست وجود sdkmanager
        if [ -f "$ANDROID_HOME/tools/bin/sdkmanager" ]; then
          echo "✅ sdkmanager is accessible at legacy path"
        else
          echo "❌ sdkmanager symlink failed!"
          exit 1
        fi
        
        # قبول لایسنس‌ها
        yes | $ANDROID_HOME/cmdline-tools/latest/bin/sdkmanager --licenses || true
        
        echo "✅ Android SDK setup complete"
    
    - name: 📥 Pre-download Android NDK r25b
      run: |
        mkdir -p ~/.buildozer/android/platform
        cd ~/.buildozer/android/platform
        if [ ! -f "android-ndk-r25b-linux.zip" ]; then
          echo "📥 Downloading Android NDK r25b..."
          wget -q https://dl.google.com/android/repository/android-ndk-r25b-linux.zip
          echo "✅ NDK downloaded"
        else
          echo "✅ NDK already cached"
        fi
    
    - name: ✅ Verify SDK Setup Before Build
      run: |
        export ANDROID_HOME=$HOME/.buildozer/android/platform/android-sdk
        echo "🔍 Checking SDK paths..."
        ls -la $ANDROID_HOME/tools/bin/ || echo "❌ tools/bin not found"
        ls -la $ANDROID_HOME/cmdline-tools/latest/bin/ || echo "❌ cmdline-tools not found"
        
        if [ -f "$ANDROID_HOME/tools/bin/sdkmanager" ]; then
          echo "✅ sdkmanager is ready!"
        else
          echo "❌ sdkmanager still missing!"
          exit 1
        fi
    
    - name: 🏗️ Build APK
      run: |
        buildozer -v android debug
    
    - name: 📤 Upload APK as Artifact
      uses: actions/upload-artifact@v4
      with:
        name: khanjar-apk
        path: bin/*.apk
    
    - name: ✅ Build Complete
      run: |
        echo "🎉 APK ساخته شد!"
        ls -lh bin/
